[manifest]
version = "1.0.0"
dump_lua = true
priority = 2147483647

# Transcendence ambiance
[[patches]]
[patches.pattern]
target = "functions/misc_functions.lua"
pattern = "G.ARGS.score_intensity.organ = G.video_organ or G.ARGS.score_intensity.required_score > 0 and math.max(math.min(0.4, 0.1*math.log(G.ARGS.score_intensity.earned_score/(G.ARGS.score_intensity.required_score+1), 5)),0.) or 0"
position = "at"
payload = '''
	if Big.arrow and G.GAME.blind and to_big(G.GAME.blind.chips or 0) > to_big(0) then
		local notzero = to_big(G.ARGS.score_intensity.required_score) > to_big(0)
		local curs = to_big(G.ARGS.score_intensity.earned_score)
		local reqs = to_big(G.ARGS.score_intensity.required_score+1)
	
		local googol = to_big(1e100)
		local googchime = to_big(googol:arrow(1, 10))
		local megagoog = googchime:arrow(2, googchime)
		
		local req0 = math.max(math.min(1, 0.1*math.log(curs/(reqs+1), 5)),0.) or 0
		local req1 = math.max(math.min(1, 0.1*math.log(curs/(reqs*1e100), 5)),0.)
		local req2 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs^1e100), 5)),0.))
		local req3 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(2, googol)), 5)),0.))
		local req4 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(3, googol)), 5)),0.))
		local req5 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(4, googol)), 5)),0.))
		local req6 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(5, googol)), 5)),0.))
		local req7 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(6, googol)), 5)),0.))
		local req8 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(7, googchime)), 5)),0.))
		local req9 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(10, googchime)), 5)),0.))
		local req10 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(70, googchime)), 5)),0.))
		local req11 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(200, googchime)), 5)),0.))
		local req12 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(1000, googchime)), 5)),0.))
		-- using fake score requirements because laggy
		local req13 = to_big(math.max(math.min(1, 0.1*math.log(curs/(googchime:arrow(2000, googchime)), 5)),0.))
		local req14 = to_big(math.max(math.min(1, 0.1*math.log(curs/(googchime:arrow(3000, googchime)), 5)),0.))
		local req15 = to_big(math.max(math.min(1, 0.1*math.log(curs/(googchime:arrow(4000, googchime)), 5)),0.))
	
		if not G.ARGS.score_intensity.tran1 then 
			G.ARGS.score_intensity.tran1 = 0 
		end
		if not G.ARGS.score_intensity.tran2 then 
			G.ARGS.score_intensity.tran2 = 0 
		end
		if not G.ARGS.score_intensity.tran3 then 
			G.ARGS.score_intensity.tran3 = 0 
		end
		if not G.ARGS.score_intensity.tran4 then 
			G.ARGS.score_intensity.tran4 = 0 
		end
		if not G.ARGS.score_intensity.tran5 then 
			G.ARGS.score_intensity.tran5 = 0 
		end
		if not G.ARGS.score_intensity.tran6 then 
			G.ARGS.score_intensity.tran6 = 0 
		end
		if not G.ARGS.score_intensity.tran7 then 
			G.ARGS.score_intensity.tran7 = 0 
		end
		if not G.ARGS.score_intensity.tran8 then 
			G.ARGS.score_intensity.tran8 = 0 
		end
		if not G.ARGS.score_intensity.tran9 then 
			G.ARGS.score_intensity.tran9 = 0 
		end
		if not G.ARGS.score_intensity.tran10 then 
			G.ARGS.score_intensity.tran10 = 0 
		end
		if not G.ARGS.score_intensity.tran11 then 
			G.ARGS.score_intensity.tran11 = 0 
		end
		if not G.ARGS.score_intensity.tran12 then 
			G.ARGS.score_intensity.tran12 = 0 
		end
		if not G.ARGS.score_intensity.tran13 then 
			G.ARGS.score_intensity.tran13 = 0 
		end
		if not G.ARGS.score_intensity.tran14 then 
			G.ARGS.score_intensity.tran14 = 0 
		end
		if not G.ARGS.score_intensity.tran15 then 
			G.ARGS.score_intensity.tran15 = 0 
		end
		if not G.ARGS.score_intensity.transcendence then
			G.ARGS.score_intensity.transcendence = 0
		end
		
		G.ARGS.score_intensity.tran15 = notzero and req15:to_number() or 0
		
		G.ARGS.score_intensity.tran14 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and notzero) and req14:to_number() or 0
		
		G.ARGS.score_intensity.tran13 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and notzero) and req13:to_number() or 0
		
		G.ARGS.score_intensity.tran12 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and notzero) and req12:to_number() or 0
		
		G.ARGS.score_intensity.tran11 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and notzero) and req11:to_number() or 0
		
		G.ARGS.score_intensity.tran10 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and notzero) and req10:to_number() or 0
		
		G.ARGS.score_intensity.tran9 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and notzero) and req9:to_number() or 0
		
		G.ARGS.score_intensity.tran8 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and notzero) and req8:to_number() or 0
		
		G.ARGS.score_intensity.tran7 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran8 or 0) <= 0.05 and notzero) and req7:to_number() or 0
		
		G.ARGS.score_intensity.tran6 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran8 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran7 or 0) <= 0.05 and notzero) and req6:to_number() or 0
		
		G.ARGS.score_intensity.tran5 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran8 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran7 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran6 or 0) <= 0.05 and notzero) and req5:to_number() or 0
		
		G.ARGS.score_intensity.tran4 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran8 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran7 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran6 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran5 or 0) <= 0.05 and notzero) and req4:to_number() or 0
	
		G.ARGS.score_intensity.tran3 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran8 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran7 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran6 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran4 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran5 or 0) <= 0.05 and	notzero) and req3:to_number() or 0
		
		G.ARGS.score_intensity.tran2 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran8 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran7 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran6 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran5 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran4 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran3 or 0) <= 0.05 and notzero) and req2:to_number() or 0
		
		G.ARGS.score_intensity.tran1 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran8 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran7 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran6 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran5 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran4 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran3 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran2 or 0) <= 0.05 and notzero) and req1 or 0
		
		G.ARGS.score_intensity.organ = G.video_organ or 
			((
			(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran8 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran7 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran6 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran5 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran4 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran3 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran2 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran1 or 0) <= 0.05 and notzero) and req0) or 0
		end

		if G.ARGS.score_intensity.tran1 < 1 then
			G.ARGS.score_intensity.transcendence = 0
		end
		if G.ARGS.score_intensity.tran1 > 0 then
			G.ARGS.score_intensity.transcendence = 1
		end
		if G.ARGS.score_intensity.tran2 > 0 then
			G.ARGS.score_intensity.transcendence = 2
		end
		if G.ARGS.score_intensity.tran3 > 0 then
			G.ARGS.score_intensity.transcendence = 3
		end
		if G.ARGS.score_intensity.tran4 > 0 then
			G.ARGS.score_intensity.transcendence = 4
		end
		if G.ARGS.score_intensity.tran5 > 0 then
			G.ARGS.score_intensity.transcendence = 5
		end
		if G.ARGS.score_intensity.tran6 > 0 then
			G.ARGS.score_intensity.transcendence = 6
		end
		if G.ARGS.score_intensity.tran7 > 0 then
			G.ARGS.score_intensity.transcendence = 7
		end
		if G.ARGS.score_intensity.tran8 > 0 then
			G.ARGS.score_intensity.transcendence = 8
		end
		if G.ARGS.score_intensity.tran9 > 0 then
			G.ARGS.score_intensity.transcendence = 9
		end
		if G.ARGS.score_intensity.tran10 > 0 then
			G.ARGS.score_intensity.transcendence = 10
		end
		if G.ARGS.score_intensity.tran11 > 0 then
			G.ARGS.score_intensity.transcendence = 11
		end
		if G.ARGS.score_intensity.tran12 > 0 then
			G.ARGS.score_intensity.transcendence = 12
		end
		if G.ARGS.score_intensity.tran13 > 0 then
			G.ARGS.score_intensity.transcendence = 13
		end
		if G.ARGS.score_intensity.tran14 > 0 then
			G.ARGS.score_intensity.transcendence = 14
		end
		if G.ARGS.score_intensity.tran15 > 0 then
			G.ARGS.score_intensity.transcendence = 15
		end
	
		notzero = nil
		curs = nil
		reqs = nil
		googol = nil
		googchime = nil
		megagoog = nil
		req1 = nil
		req2 = nil
		req3 = nil
		req4 = nil
		req5 = nil
		req6 = nil
		req7 = nil
		req8 = nil
		req9 = nil
		req10 = nil
		req11 = nil
		req12 = nil
		re13 = nil
		req14 = nil
		req15 = nil
		
		if may and type(may) == 'table' then
			may.transcendence = G.ARGS.score_intensity.transcendence or 0 
		end
	else
		G.ARGS.score_intensity.organ = G.video_organ or to_number(G.ARGS.score_intensity.required_score) > 0 and math.max(math.min(0.4, 0.1*math.log(G.ARGS.score_intensity.earned_score/(G.ARGS.score_intensity.required_score+1), 5)),0.) or 0
	end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "functions/misc_functions.lua"
pattern = "G.ARGS.score_intensity.organ = G.video_organ or to_big(G.ARGS.score_intensity.required_score) > to_big(0) and math.max(math.min(0.4, 0.1*math.log(G.ARGS.score_intensity.earned_score/(G.ARGS.score_intensity.required_score+1), 5)),0.) or 0"
position = "at"
payload = '''
	if Big.arrow and G.GAME.blind and to_big(G.GAME.blind.chips or 0) > to_big(0) then
		local notzero = to_big(G.ARGS.score_intensity.required_score) > to_big(0)
		local curs = to_big(G.ARGS.score_intensity.earned_score)
		local reqs = to_big(G.ARGS.score_intensity.required_score+1)
	
		local googol = to_big(1e100)
		local googchime = to_big(googol:arrow(1, 10))
		local megagoog = googchime:arrow(2, googchime)
		
		local req0 = math.max(math.min(1, 0.1*math.log(curs/(reqs+1), 5)),0.) or 0
		local req1 = math.max(math.min(1, 0.1*math.log(curs/(reqs*1e100), 5)),0.)
		local req2 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs^1e100), 5)),0.))
		local req3 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(2, googol)), 5)),0.))
		local req4 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(3, googol)), 5)),0.))
		local req5 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(4, googol)), 5)),0.))
		local req6 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(5, googol)), 5)),0.))
		local req7 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(6, googol)), 5)),0.))
		local req8 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(7, googchime)), 5)),0.))
		local req9 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(10, googchime)), 5)),0.))
		local req10 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(70, googchime)), 5)),0.))
		local req11 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(200, googchime)), 5)),0.))
		local req12 = to_big(math.max(math.min(1, 0.1*math.log(curs/(reqs:arrow(1000, googchime)), 5)),0.))
		-- using fake score requirements because laggy
		local req13 = to_big(math.max(math.min(1, 0.1*math.log(curs/(googchime:arrow(2000, googchime)), 5)),0.))
		local req14 = to_big(math.max(math.min(1, 0.1*math.log(curs/(googchime:arrow(3000, googchime)), 5)),0.))
		local req15 = to_big(math.max(math.min(1, 0.1*math.log(curs/(googchime:arrow(4000, googchime)), 5)),0.))
	
		if not G.ARGS.score_intensity.tran1 then 
			G.ARGS.score_intensity.tran1 = 0 
		end
		if not G.ARGS.score_intensity.tran2 then 
			G.ARGS.score_intensity.tran2 = 0 
		end
		if not G.ARGS.score_intensity.tran3 then 
			G.ARGS.score_intensity.tran3 = 0 
		end
		if not G.ARGS.score_intensity.tran4 then 
			G.ARGS.score_intensity.tran4 = 0 
		end
		if not G.ARGS.score_intensity.tran5 then 
			G.ARGS.score_intensity.tran5 = 0 
		end
		if not G.ARGS.score_intensity.tran6 then 
			G.ARGS.score_intensity.tran6 = 0 
		end
		if not G.ARGS.score_intensity.tran7 then 
			G.ARGS.score_intensity.tran7 = 0 
		end
		if not G.ARGS.score_intensity.tran8 then 
			G.ARGS.score_intensity.tran8 = 0 
		end
		if not G.ARGS.score_intensity.tran9 then 
			G.ARGS.score_intensity.tran9 = 0 
		end
		if not G.ARGS.score_intensity.tran10 then 
			G.ARGS.score_intensity.tran10 = 0 
		end
		if not G.ARGS.score_intensity.tran11 then 
			G.ARGS.score_intensity.tran11 = 0 
		end
		if not G.ARGS.score_intensity.tran12 then 
			G.ARGS.score_intensity.tran12 = 0 
		end
		if not G.ARGS.score_intensity.tran13 then 
			G.ARGS.score_intensity.tran13 = 0 
		end
		if not G.ARGS.score_intensity.tran14 then 
			G.ARGS.score_intensity.tran14 = 0 
		end
		if not G.ARGS.score_intensity.tran15 then 
			G.ARGS.score_intensity.tran15 = 0 
		end
		if not G.ARGS.score_intensity.transcendence then
			G.ARGS.score_intensity.transcendence = 0
		end
		
		G.ARGS.score_intensity.tran15 = notzero and req15:to_number() or 0
		
		G.ARGS.score_intensity.tran14 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and notzero) and req14:to_number() or 0
		
		G.ARGS.score_intensity.tran13 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and notzero) and req13:to_number() or 0
		
		G.ARGS.score_intensity.tran12 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and notzero) and req12:to_number() or 0
		
		G.ARGS.score_intensity.tran11 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and notzero) and req11:to_number() or 0
		
		G.ARGS.score_intensity.tran10 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and notzero) and req10:to_number() or 0
		
		G.ARGS.score_intensity.tran9 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and notzero) and req9:to_number() or 0
		
		G.ARGS.score_intensity.tran8 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and notzero) and req8:to_number() or 0
		
		G.ARGS.score_intensity.tran7 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran8 or 0) <= 0.05 and notzero) and req7:to_number() or 0
		
		G.ARGS.score_intensity.tran6 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran8 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran7 or 0) <= 0.05 and notzero) and req6:to_number() or 0
		
		G.ARGS.score_intensity.tran5 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran8 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran7 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran6 or 0) <= 0.05 and notzero) and req5:to_number() or 0
		
		G.ARGS.score_intensity.tran4 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran8 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran7 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran6 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran5 or 0) <= 0.05 and notzero) and req4:to_number() or 0
	
		G.ARGS.score_intensity.tran3 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran8 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran7 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran6 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran4 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran5 or 0) <= 0.05 and	notzero) and req3:to_number() or 0
		
		G.ARGS.score_intensity.tran2 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran8 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran7 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran6 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran5 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran4 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran3 or 0) <= 0.05 and notzero) and req2:to_number() or 0
		
		G.ARGS.score_intensity.tran1 = (
		(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran8 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran7 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran6 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran5 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran4 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran3 or 0) <= 0.05 and
		(G.ARGS.score_intensity.tran2 or 0) <= 0.05 and notzero) and req1 or 0
		
		if may.TrAudio == 0 then
			G.ARGS.score_intensity.organ = G.video_organ or notzero and to_big(req0):to_number() or 0
		else
		G.ARGS.score_intensity.organ = G.video_organ or 
			((
			(G.ARGS.score_intensity.tran15 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran14 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran13 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran12 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran11 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran10 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran9 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran8 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran7 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran6 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran5 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran4 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran3 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran2 or 0) <= 0.05 and
			(G.ARGS.score_intensity.tran1 or 0) <= 0.05 and notzero) and req0) or 0
		end

		if G.ARGS.score_intensity.tran1 < 1 then
			G.ARGS.score_intensity.transcendence = 0
		end
		if G.ARGS.score_intensity.tran1 > 0 then
			G.ARGS.score_intensity.transcendence = 1
		end
		if G.ARGS.score_intensity.tran2 > 0 then
			G.ARGS.score_intensity.transcendence = 2
		end
		if G.ARGS.score_intensity.tran3 > 0 then
			G.ARGS.score_intensity.transcendence = 3
		end
		if G.ARGS.score_intensity.tran4 > 0 then
			G.ARGS.score_intensity.transcendence = 4
		end
		if G.ARGS.score_intensity.tran5 > 0 then
			G.ARGS.score_intensity.transcendence = 5
		end
		if G.ARGS.score_intensity.tran6 > 0 then
			G.ARGS.score_intensity.transcendence = 6
		end
		if G.ARGS.score_intensity.tran7 > 0 then
			G.ARGS.score_intensity.transcendence = 7
		end
		if G.ARGS.score_intensity.tran8 > 0 then
			G.ARGS.score_intensity.transcendence = 8
		end
		if G.ARGS.score_intensity.tran9 > 0 then
			G.ARGS.score_intensity.transcendence = 9
		end
		if G.ARGS.score_intensity.tran10 > 0 then
			G.ARGS.score_intensity.transcendence = 10
		end
		if G.ARGS.score_intensity.tran11 > 0 then
			G.ARGS.score_intensity.transcendence = 11
		end
		if G.ARGS.score_intensity.tran12 > 0 then
			G.ARGS.score_intensity.transcendence = 12
		end
		if G.ARGS.score_intensity.tran13 > 0 then
			G.ARGS.score_intensity.transcendence = 13
		end
		if G.ARGS.score_intensity.tran14 > 0 then
			G.ARGS.score_intensity.transcendence = 14
		end
		if G.ARGS.score_intensity.tran15 > 0 then
			G.ARGS.score_intensity.transcendence = 15
		end
	
		notzero = nil
		curs = nil
		reqs = nil
		googol = nil
		googchime = nil
		megagoog = nil
		req1 = nil
		req2 = nil
		req3 = nil
		req4 = nil
		req5 = nil
		req6 = nil
		req7 = nil
		req8 = nil
		req9 = nil
		req10 = nil
		req11 = nil
		req12 = nil
		re13 = nil
		req14 = nil
		req15 = nil
		
		if may and type(may) == 'table' then
			may.transcendence = G.ARGS.score_intensity.transcendence or 0 
		end
	else
		G.ARGS.score_intensity.organ = G.video_organ or to_number(G.ARGS.score_intensity.required_score) > 0 and math.max(math.min(0.4, 0.1*math.log(G.ARGS.score_intensity.earned_score/(G.ARGS.score_intensity.required_score+1), 5)),0.) or 0
	end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "functions/misc_functions.lua"
pattern = "ambientOrgan1 = {volfunc = function(_prev_volume) return _prev_volume*(1 - dt) + dt*0.6*(G.SETTINGS.SOUND.music_volume + 100)/200*(G.ARGS.score_intensity.organ) end},"
position = "after"
payload = '''
	may_tran1 = {volfunc = function(_prev_volume) return _prev_volume*(1 - dt) + dt*0.6*(G.SETTINGS.SOUND.game_sounds_volume + 100)/200*((G.ARGS.score_intensity.tran1 or 0) * 6) * (may.conf.TrAudio * 0.01) end},
	may_tran2 = {volfunc = function(_prev_volume) return _prev_volume*(1 - dt) + dt*0.6*(G.SETTINGS.SOUND.game_sounds_volume + 100)/200*((G.ARGS.score_intensity.tran2 or 0) * 10) * (may.conf.TrAudio * 0.01) end},
	may_tran3 = {volfunc = function(_prev_volume) return _prev_volume*(1 - dt) + dt*0.6*(G.SETTINGS.SOUND.game_sounds_volume + 100)/200*((G.ARGS.score_intensity.tran3 or 0) * 10) * (may.conf.TrAudio * 0.01) end},
	may_tran4 = {volfunc = function(_prev_volume) return _prev_volume*(1 - dt) + dt*0.6*(G.SETTINGS.SOUND.game_sounds_volume + 100)/200*((G.ARGS.score_intensity.tran4 or 0) * 6) * (may.conf.TrAudio * 0.01) end},
	may_tran5 = {volfunc = function(_prev_volume) return _prev_volume*(1 - dt) + dt*0.6*(G.SETTINGS.SOUND.game_sounds_volume + 100)/200*((G.ARGS.score_intensity.tran5 or 0) * 7) * (may.conf.TrAudio * 0.01) end},
	may_tran6 = {volfunc = function(_prev_volume) return _prev_volume*(1 - dt) + dt*0.6*(G.SETTINGS.SOUND.game_sounds_volume + 100)/200*((G.ARGS.score_intensity.tran6 or 0) * 8) * (may.conf.TrAudio * 0.01) end},
	may_tran7 = {volfunc = function(_prev_volume) return _prev_volume*(1 - dt) + dt*0.6*(G.SETTINGS.SOUND.game_sounds_volume + 100)/200*((G.ARGS.score_intensity.tran7 or 0) * 8) * (may.conf.TrAudio * 0.01) end},
	may_tran8 = {volfunc = function(_prev_volume) return _prev_volume*(1 - dt) + dt*0.6*(G.SETTINGS.SOUND.game_sounds_volume + 100)/200*((G.ARGS.score_intensity.tran8 or 0) * 8) * (may.conf.TrAudio * 0.01) end},
	-- the rest oif the transcendences are handled as a music tracks so they can replace the main theme
	may_ambient_mass_use = {volfunc = function(_prev_volume) return _prev_volume*(1 - dt) + dt*0.6*(G.SETTINGS.SOUND.music_volume + 100)/200*((may_mass_use_index or 0) * 8) end},
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "game.lua"
pattern = "replace_card.states.visible = false"
position = "before"
payload = '''
	if may.mode == 1 then
		replace_card:set_ability(G.P_CENTERS.m_mult, true, true)
	else
		replace_card:set_ability(G.P_CENTERS.m_may_super_mult, true, true)
	end
'''
match_indent = true
overwrite = false

# Fix hand leveling reseting mult and chips after using Planetae and such
[[patches]]
[patches.pattern]
target = "functions/common_events.lua"
pattern = "G.GAME.hands[hand].mult = math.max(G.GAME.hands[hand].s_mult + G.GAME.hands[hand].l_mult*(G.GAME.hands[hand].level - 1), 1)"
position = "at"
payload = '''
	G.GAME.hands[hand].mult = math.max(G.GAME.hands[hand].mult + to_big(G.GAME.hands[hand].l_mult)*(to_big(G.GAME.hands[hand].level) - to_big(1)), to_big(1))
'''
match_indent = true
overwrite = true

[[patches]]
[patches.pattern]
target = "functions/common_events.lua"
pattern = "G.GAME.hands[hand].chips = math.max(G.GAME.hands[hand].s_chips + G.GAME.hands[hand].l_chips*(G.GAME.hands[hand].level - 1), 0)"
position = "at"
payload = '''
	G.GAME.hands[hand].chips = math.max(G.GAME.hands[hand].chips + to_big(G.GAME.hands[hand].l_chips)*(to_big(G.GAME.hands[hand].level) - to_big(1)), to_big(0))
'''
match_indent = true
overwrite = true

# Custom score operator
[[patches]]
[patches.pattern]
target = "functions/state_events.lua"
pattern = "func = (function()  update_hand_text({delay = 0, immediate = true}, {mult = 0, chips = 0, chip_total = math.floor(hand_chips*mult), level = '', handname = ''});play_sound('button', 0.9, 0.6);return true end)"
position = "at"
payload = '''
	func = (function()  update_hand_text({delay = 0, immediate = true}, {mult = 0, chips = 0, chip_total = get_final_score(), level = '', handname = ''});play_sound('button', 0.9, 0.6);return true end)
'''
match_indent = true
overwrite = true

[[patches]]
[patches.pattern]
target = "functions/state_events.lua"
pattern = "check_and_set_high_score('hand', hand_chips*mult)"
position = "at"
payload = '''
	check_and_set_high_score('hand', get_final_score(to_big(hand_chips), to_big(mult)))
'''
match_indent = true
overwrite = true

[[patches]]
[patches.pattern]
target = "functions/state_events.lua"
pattern = "check_for_unlock({type = 'chip_score', chips = math.floor(hand_chips*mult)})"
position = "at"
payload = '''
	check_for_unlock({type = 'chip_score', chips = math.floor(get_final_score(to_big(hand_chips), to_big(mult)))})
'''
match_indent = true
overwrite = true

[[patches]]
[patches.pattern]
target = "functions/state_events.lua"
pattern = "ease_to = G.GAME.chips + math.floor(hand_chips*mult),"
position = "at"
payload = '''
	ease_to = G.GAME.chips + get_final_score(to_big(hand_chips), to_big(mult)):floor(),
'''
match_indent = true
overwrite = true

[[patches]]
[patches.pattern]
target = "functions/misc_functions.lua"
pattern = "G.ARGS.score_intensity.earned_score = G.GAME.current_round.current_hand.chips*G.GAME.current_round.current_hand.mult"
position = "at"
payload = '''
	G.ARGS.score_intensity.earned_score = get_final_score(to_big(G.GAME.current_round.current_hand.chips), to_big(G.GAME.current_round.current_hand.mult))
'''
match_indent = true

[[patches]]
[patches.regex]
target = "functions/state_events.lua"
pattern = '''hand_chips\*mult'''
position = "at"
payload = "to_big(get_final_score(to_big(hand_chips), to_big(mult)))"
match_indent = true

[[patches]]
[patches.regex]
target = "functions/state_events.lua"
pattern = '''to_big\(hand_chips\)\*mult'''
position = "at"
payload = "to_big(get_final_score(to_big(hand_chips), to_big(mult)))"
match_indent = true

[[patches]]
[patches.pattern]
target = "functions/state_events.lua"
pattern = "if to_big(hand_chips)*mult > to_big(0) then"
position = "at"
payload = '''if to_big(get_final_score(to_big(hand_chips), to_big(mult))) > to_big(0) then'''
match_indent = true

[[patches]]
[patches.pattern]
target = "game.lua"
pattern = "G.GAME.blind:load(saveTable.BLIND)"
position = "after"
payload = '''G.E_MANAGER:add_event(Event({func = function() update_score_operator(nil, true) return true end}))'''
match_indent = true

[[patches]]
[patches.pattern]
target = "functions/UI_definitions.lua"
pattern = '''{n=G.UIT.T, config={text = "X", lang = G.LANGUAGES['en-us'], scale = scale*2, colour = G.C.UI_MULT, shadow = true}},'''
position = "at"
payload = '''{n=G.UIT.T, config={id = "chipmult_op", text = "X", lang = G.LANGUAGES["en-us"], scale = scale*2, colour = G.C.UI_MULT, shadow = true}},'''
match_indent = true

[[patches]]
[patches.pattern]
target = "game.lua"
pattern = "game_chips = self.HUD:get_UIE_by_ID('chip_UI_count'),"
position = "after"
payload = '''chipmult_op = self.HUD:get_UIE_by_ID('chipmult_op'),'''
match_indent = true

# Yggdrasil compat
[[patches]]
[patches.pattern]
target = "functions/UI_definitions.lua"
pattern = '''{n=G.UIT.T, config={text = "X", lang = G.LANGUAGES['en-us'], scale = scale*1, colour = G.C.UI_MULT, shadow = true}},'''
position = "at"
payload = '''{n=G.UIT.T, config={id = "chipmult_op", text = "X", lang = G.LANGUAGES["en-us"], scale = scale*1, colour = G.C.UI_MULT, shadow = true}},'''
match_indent = true

# Intrusive PATA fix
[[patches]]
[patches.pattern]
target = 'functions/UI_definitions.lua'
pattern = '''
	local realhand = Yggdrasil and contents.hand.nodes[1] or contents.hand
	local sc = (Yggdrasil and contents.hand.nodes[1] or contents.hand).nodes[1].nodes[2].nodes[2].nodes[1].config.scale
	local exis = DynaText{string = {"X"}, pop_in_rate = 9999999, silent = true, scale = sc, colours = {G.C.UI_MULT}, shadow = true}
	realhand.nodes[1].nodes[2].nodes[2].nodes[1] = {n=G.UIT.O, config={object = exis, id = 'chips_what_mult'}}
	'''
position = 'before'
payload = '''
	local realhand = Yggdrasil and contents.hand.nodes[1] or contents.hand
	local sc = (Yggdrasil and contents.hand.nodes[1] or contents.hand).nodes[1].nodes[2].nodes[2].nodes[1].config.scale
	local exis = DynaText{string = {"X"}, pop_in_rate = 9999999, silent = true, scale = sc, colours = {G.C.UI_MULT}, shadow = true}
	realhand.nodes[1].nodes[2].nodes[2].nodes[1] = {n=G.UIT.O, config={object = exis, id = 'chipmult_op'}}
'''
match_indent = true
times = 1

# Custom hyperoperation effects

[[patches]]
[patches.pattern]
target = "functions/common_events.lua"
pattern = "local extrafunc = nil"
position = "after"
payload = '''
	if eval_type == 'hyper_chips' or eval_type == 'hyper_mult' or eval_type == 'hyperchip_mod' or eval_type == 'hypermult_mod' then
		local arrow = amt[1]
	end
	local operator_intensity = 0
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "functions/common_events.lua"
pattern = '''elseif eval_type == 'x_chips' then 
	sound = 'talisman_xchip'
	amt = amt
	text = 'X' .. amt
	colour = G.C.CHIPS
	config.type = 'fade'
	config.scale = 0.7
elseif eval_type == 'e_chips' then 
	sound = 'talisman_echip'
	amt = amt
	text = '^' .. amt
	colour = G.C.CHIPS
	config.type = 'fade'
	config.scale = 0.7
elseif eval_type == 'ee_chips' then 
	sound = 'talisman_eechip'
	amt = amt
	text = '^^' .. amt
	colour = G.C.CHIPS
	config.type = 'fade'
	config.scale = 0.7
elseif eval_type == 'eee_chips' then 
	sound = 'talisman_eeechip'
	amt = amt
	text = '^^^' .. amt
	colour = G.C.CHIPS
	config.type = 'fade'
	config.scale = 0.7
elseif eval_type == 'hyper_chips' then
	sound = 'talisman_eeechip'
	text = (amt[1] > 5 and ('{' .. tostring(amt[1]) .. '}') or string.rep('^', amt[1])) .. tostring(amt[2])
	amt = amt[2]
	colour = G.C.CHIPS
	config.type = 'fade'
	config.scale = 0.7
elseif eval_type == 'e_mult' then 
	sound = 'talisman_emult'
	amt = amt
	text = '^' .. amt .. ' Mult'
	colour = G.C.MULT
	config.type = 'fade'
	config.scale = 0.7
elseif eval_type == 'ee_mult' then 
	sound = 'talisman_eemult'
	amt = amt
	text = '^^' .. amt .. ' Mult'
	colour = G.C.MULT
	config.type = 'fade'
	config.scale = 0.7
elseif eval_type == 'eee_mult' then 
	sound = 'talisman_eeemult'
	amt = amt
	text = '^^^' .. amt .. ' Mult'
	colour = G.C.MULT
	config.type = 'fade'
	config.scale = 0.7
elseif eval_type == 'hyper_mult' or eval_type == 'hypermult_mod' then 
	sound = 'talisman_eeemult'
	text = (amt[1] > 5 and ('{' .. tostring(amt[1]) .. '}') or string.rep('^', amt[1])) .. tostring(amt[2]) .. ' Mult'
	amt = amt[2]
	colour = G.C.MULT
	config.type = 'fade'
	config.scale = 0.7'''
position = "at"
payload = '''
elseif eval_type == 'x_chips' then 
	sound = 'talisman_xchip'
	amt = amt
	text = 'X'..amt..' Chips'
	colour = G.C.CHIPS
	config.type = 'fade'
	config.scale = 0.7
elseif eval_type == 'e_chips' then 
	sound = 'talisman_echip'
	amt = amt
	text = '^'..amt..' Chips'
	colour = G.C.CHIPS
	config.type = 'fade'
	config.scale = 0.7
	operator_intensity = 0.1
elseif eval_type == 'ee_chips' then 
	sound = 'talisman_eechip'
	amt = amt
	text = '^^'..amt..' Chips'
	colour = G.C.CHIPS
	config.type = 'fade'
	config.scale = 0.7
	operator_intensity = 0.3
elseif eval_type == 'eee_chips' then 
	sound = 'talisman_eeechip'
	amt = amt
	text = '^^^' ..amt..' Chips'
	colour = G.C.CHIPS
	config.type = 'fade'
	config.scale = 0.7
	operator_intensity = 0.5
elseif eval_type == 'hyper_chips' or eval_type == 'hyperchip_mod' then
	sound = may.get_operation_sound(amt[1], 'chips')
	text = (amt[1] > 5 and ('{' .. tostring(amt[1]) .. '}') or string.rep('^', amt[1])) .. tostring(amt[2])..' Chips'
	colour = G.C.CHIPS
	config.type = 'fade'
	config.scale = 0.2+amt[1]^0.001
	operator_intensity = 0.5+math.log(amt[1], 1000)
	amt = amt[2]
elseif eval_type == 'e_mult' then 
	sound = 'talisman_emult'
	amt = amt
	text = '^' .. amt .. ' Mult'
	colour = G.C.MULT
	config.type = 'fade'
	config.scale = 0.7
	operator_intensity = 0.1
elseif eval_type == 'ee_mult' then 
	sound = 'talisman_eemult'
	amt = amt
	text = '^^' .. amt .. ' Mult'
	colour = G.C.MULT
	config.type = 'fade'
	config.scale = 0.7
	operator_intensity = 0.3
elseif eval_type == 'eee_mult' then 
	sound = 'talisman_eeemult'
	amt = amt
	text = '^^^' .. amt .. ' Mult'
	colour = G.C.MULT
	config.type = 'fade'
	config.scale = 0.7
	operator_intensity = 0.5
elseif eval_type == 'hyper_mult' then 
	sound = may.get_operation_sound(amt[1], 'mult')
	text = (amt[1] > 5 and ('{' .. tostring(amt[1]) .. '}') or string.rep('^', amt[1])) .. tostring(amt[2]) .. ' Mult'
	colour = G.C.MULT
	config.type = 'fade'
	config.scale = 0.2+amt[1]^0.001
	operator_intensity = 0.5+math.log(amt[1], 1000)
	amt = amt[2]
 elseif eval_type == 'x_dollars' then 
        sound = 'may_bigmoney'
        amt = amt
        text = 'X'..localize("$")..tostring(math.abs(amt))
        colour = amt < 1 and G.C.RED or G.C.MONEY
'''
match_indent = true
overwrite = true

# Failsafe because custom hyperoperation sound patch is very fussy
[[patches]] 
[patches.pattern]
target = "functions/common_events.lua"
pattern = '''
	elseif eval_type == 'hyper_mult' then 
		sound = 'talisman_eeemult'
'''
position = "at"
payload = '''
	elseif eval_type == 'hyper_mult' then 
		sound = may.get_operation_sound(amt[1], 'mult')
'''
match_indent = true
overwrite = true

[[patches]] 
[patches.pattern]
target = "functions/common_events.lua"
pattern = '''
	elseif eval_type == 'hyper_chips' then 
		sound = 'talisman_eeechip'
'''
position = "at"
payload = '''
	elseif eval_type == 'hyper_chips' then 
		sound = may.get_operation_sound(amt[1], 'chips')
'''
match_indent = true
overwrite = true

[[patches]] 
[patches.pattern]
target = "functions/common_events.lua"
pattern = '''
if not extra or not extra.no_juice then
                card:juice_up(0.6, 0.1)
                G.ROOM.jiggle = G.ROOM.jiggle + 0.7
            end
        else
            G.E_MANAGER:add_event(Event({ --Add bonus chips from this card
                    trigger = 'before',
                    delay = delay,
                    func = function()
                    if extrafunc then extrafunc() end
                    attention_text({
                        text = text,
                        scale = config.scale or 1, 
                        hold = delay - 0.2,
                        backdrop_colour = colour,
                        align = card_aligned,
                        major = card,
                        offset = {x = 0, y = y_off}
                    })
                    play_sound(sound, 0.8+percent*0.2, volume)
                    if not extra or not extra.no_juice then
                        card:juice_up(0.6, 0.1)
                        G.ROOM.jiggle = G.ROOM.jiggle + 0.7
                    end
'''
position = "at"
payload = '''
if not extra or not extra.no_juice then
	card:juice_up(0.6 + ((math.min(3, amt^0.1) + (arrow or 0)^0.01 + operator_intensity)^0.1), 0.1 + math.min(3, amt^0.1) + (arrow or 0)^0.01 + operator_intensity)
	G.ROOM.jiggle = G.ROOM.jiggle + 0.7 + math.min(3, amt^0.1) + (arrow or 0)^0.01 + operator_intensity
end
	else
	G.E_MANAGER:add_event(Event({trigger = 'before',delay = delay,func = function()
		if extrafunc then extrafunc() end
		attention_text({
			text = text,
			scale = config.scale or 1, 
			hold = delay - 0.2,
			backdrop_colour = colour,
			align = card_aligned,
			major = card,
			offset = {x = 0, y = y_off}
		})
		play_sound(sound, 0.8+percent*0.2, volume)
		if not extra or not extra.no_juice then
			card:juice_up(0.6 + ((math.min(3, amt^0.1) + (arrow or 0)^0.01 + operator_intensity)^0.1), 0.1 + math.min(3, amt^0.1) + (arrow or 0)^0.01 + operator_intensity)
			G.ROOM.jiggle = G.ROOM.jiggle + 0.7 + math.min(3, amt^0.1) + (arrow or 0)^0.01 + operator_intensity
		end
'''
match_indent = true
overwrite = true

# Custom operations

[[patches]] 
[patches.pattern]
target = "functions/state_events.lua"
pattern = "if effects.jokers.Xmult_mod then mult = mod_mult(mult*effects.jokers.Xmult_mod);extras.mult = true  end"
position = "at"
payload = '''
if effects.jokers.Emult_mod then mult = mod_mult(mult^effects.jokers.Emult_mod);extras.mult = true end
if effects.jokers.EEmult_mod then mult = mod_mult(mult:arrow(2, effects.jokers.EEmult_mod));extras.mult = true end
if effects.jokers.EEEmult_mod then mult = mod_mult(mult:arrow(3, effects.jokers.EEEmult_mod));extras.mult = true end
if effects.jokers.hypermult_mod and type(effects.jokers.hypermult_mod) == 'table' then mult = mod_mult(mult:arrow(effects.jokers.hypermult_mod[1], effects.jokers.hypermult_mod[2]));extras.mult = true end

if effects.jokers.Xchip_mod then hand_chips = mod_chips(hand_chips*effects.jokers.Xchip_mod);extras.hand_chips = true end
if effects.jokers.Echip_mod then hand_chips = mod_chips(hand_chips^effects.jokers.Echip_mod);extras.hand_chips = true end
if effects.jokers.EEchip_mod then hand_chips = mod_chips(hand_chips:arrow(2, effects.jokers.EEchip_mod));extras.hand_chips = true end
if effects.jokers.EEEchip_mod then hand_chips = mod_chips(hand_chips:arrow(3, effects.jokers.EEEchip_mod));extras.hand_chips = true end
if effects.jokers.hyperchip_mod and type(effects.jokers.hyperchip_mod) == 'table' then hand_chips = mod_chips(hand_chips:arrow(effects.jokers.hyperchip_mod[1], effects.jokers.hyperchip_mod[2]));extras.hand_chips = true end
'''
match_indent = true
overwrite = true

[[patches]] 
[patches.pattern]
target = "functions/state_events.lua"
pattern = "if effect.Xmult_mod then mult = mod_mult(mult*effect.Xmult_mod);extras.mult = true  end"
position = "at"
payload = '''
if effects.Emult_mod then mult = mod_mult(mult^effects.Emult_mod);extras.mult = true end
if effects.EEmult_mod then mult = mod_mult(mult:arrow(2, effects.EEmult_mod));extras.mult = true end
if effects.EEEmult_mod then mult = mod_mult(mult:arrow(3, effects.EEEmult_mod));extras.mult = true end
if effects.hypermult_mod and type(effects.hypermult_mod) == 'table' then mult = mod_mult(mult:arrow(effects.hypermult_mod[1], effects.hypermult_mod[2]));extras.mult = true end

if effects.Xchip_mod then hand_chips = mod_chips(hand_chips*effects.Xchip_mod);extras.hand_chips = true end
if effects.Echip_mod then hand_chips = mod_chips(hand_chips^effects.Echip_mod);extras.hand_chips = true end
if effects.EEchip_mod then hand_chips = mod_chips(hand_chips:arrow(2, effects.EEchip_mod));extras.hand_chips = true end
if effects.EEEchip_mod then hand_chips = mod_chips(hand_chips:arrow(3, effects.EEEchip_mod));extras.hand_chips = true end
if effects.hyperchip_mod and type(effects.hyperchip_mod) == 'table' then hand_chips = mod_chips(hand_chips:arrow(effects.hyperchip_mod[1], effects.hyperchip_mod[2]));extras.hand_chips = true end
'''
match_indent = true
overwrite = true

[[patches]]
[patches.pattern]
target = "card.lua"
pattern = "if effect.Xmult_mod then mult = mod_mult(mult*effect.Xmult_mod);extras.mult = true  end"
position = "after"
payload = '''
e_mult = center.config.Emult or 0,
ee_mult = center.config.EEmult or 0,
eee_mult = center.config.EEEmult or 0,
hyper_mult = type(center.config.Hmult) == 'table' and center.config.Hmult or {0, 0},

x_chips = center.config.Xchips or 0,
e_chips = center.config.Echips or 0,
ee_chips = center.config.EEchips or 0,
eee_chips = center.config.EEEchips or 0,
hyper_chips = type(center.config.Hchips) == 'table' and center.config.Hchips or {0, 0},
'''
match_indent = true
overwrite = true

[[patches]]
[patches.pattern]
target = "functions/common_events.lua"
pattern = '''local x_chips = card:get_chip_x_bonus()
if x_chips > 0 then
	ret.x_chips = x_chips
end

local e_chips = card:get_chip_e_bonus()
if e_chips > 0 then
	ret.e_chips = e_chips
end

local ee_chips = card:get_chip_ee_bonus()
if ee_chips > 0 then
	ret.ee_chips = ee_chips
end

local eee_chips = card:get_chip_eee_bonus()
if eee_chips > 0 then
	ret.eee_chips = eee_chips
end

local hyper_chips = card:get_chip_hyper_bonus()
if type(hyper_chips) == 'table' and hyper_chips[1] > 0 and hyper_chips[2] > 0 then
	ret.hyper_chips = hyper_chips
end

local e_mult = card:get_chip_e_mult()
if e_mult > 0 then
	ret.e_mult = e_mult
end

local ee_mult = card:get_chip_ee_mult()
if ee_mult > 0 then
	ret.ee_mult = ee_mult
end

local eee_mult = card:get_chip_eee_mult()
if eee_mult > 0 then
	ret.eee_mult = eee_mult
end

local hyper_mult = card:get_chip_hyper_mult()
if type(hyper_mult) == 'table' and hyper_mult[1] > 0 and hyper_mult[2] > 0 then
	ret.hyper_mult = hyper_mult
end
'''
position = "at"
payload = '''
local x_chips = card:get_chip_x_bonus()
if x_chips > 0 then
	ret.x_chips = x_chips
end

local e_chips = card:get_chip_e_bonus()
if e_chips > 0 then
	ret.e_chips = e_chips
end

local ee_chips = card:get_chip_ee_bonus()
if ee_chips > 0 then
	ret.ee_chips = ee_chips
end

local eee_chips = card:get_chip_eee_bonus()
if eee_chips > 0 then
	ret.eee_chips = eee_chips
end

local hyper_chips = card:get_chip_hyper_bonus()
if type(hyper_chips) == 'table' and hyper_chips[1] > 0 and hyper_chips[2] > 0 then
	ret.hyper_chips = hyper_chips
end

local e_mult = card:get_chip_e_mult()
if e_mult > 0 then
	ret.e_mult = e_mult
end

local ee_mult = card:get_chip_ee_mult()
if ee_mult > 0 then
	ret.ee_mult = ee_mult
end

local eee_mult = card:get_chip_eee_mult()
if eee_mult > 0 then
	ret.eee_mult = eee_mult
end

local hyper_mult = card:get_chip_hyper_mult()
if type(hyper_mult) == 'table' and hyper_mult[1] > 0 and hyper_mult[2] > 0 then
	ret.hyper_mult = hyper_mult
end
'''
match_indent = true

[[patches]] 
[patches.pattern]
target = "conf.lua"
pattern = "t.title = 'Balatro'"
position = "at"
payload = "t.title = 'Balatro' -- Wanted to have Balatro Mayhem as the game title but that's cheesy"
match_indent = true

# Playing card chip multiplier (brown deck, white seal)
[[patches]] 
[patches.pattern]
target = "card.lua"
pattern = "return self.base.nominal + self.ability.bonus + (self.ability.perma_bonus or 0)"
position = "at"
payload = '''
if not G.GAME then
	return (self.base.nominal + self.ability.bonus + (self.ability.perma_bonus or 0)) * (self.ability.nominal_multiplier or 1)
else
	return (self.base.nominal + self.ability.bonus + (self.ability.perma_bonus or 0)) * (G.GAME.playing_card_multiplier or 1) * (self.ability.nominal_multiplier or 1)
end'''
match_indent = true
overwrite = true

[[patches]] 
[patches.pattern]
target = "card.lua"
pattern = "nominal_chips = self.base.nominal > 0 and self.base.nominal or nil,"
position = "at"
payload = "nominal_chips = (self.base.nominal > 0 and self.base.nominal or 0) * (G.GAME and G.GAME.playing_card_multiplier or 1) * (self.ability.nominal_multiplier or 1),"
match_indent = true
overwrite = true

# Custom operations
[[patches]]
[patches.pattern]
target = "functions/state_events.lua"
pattern = "update_hand_text({delay = 0}, {chips = extras.hand_chips and hand_chips, mult = extras.mult and mult})"
position = "before"
payload = '''
if effects.jokers.xdollars_mod then dollars = xdollars_mod;extras.dollars = true end

if effects.FACTmult_mod then mult = mod_mult(to_big(mult):fact(effects.FACTmult_mod));extras.mult = true end
if effects.EXPOFACTmult_mod then mult = mod_mult(to_big(mult):expofact(effects.EXPOFACTmult_mod));extras.mult = true end
if effects.APEmult_mod then mult = mod_mult(to_big(mult):aperiorate(effects.APEmult_mod));extras.mult = true end

if effects.FACTchip_mod then chips = mod_chips(to_big(chips):fact(effects.FACTchip_mod));extras.chip = true end
if effects.EXPOFACTchip_mod then mult = mod_mult(to_big(mult):expofact(effects.EXPOFACTchip_mod));extras.chip = true end
if effects.APEchip_mod then chips = mod_chips(to_big(chips):aperiorate(effects.APEchip_mod));extras.chip = true end
'''
match_indent = true

# Glitchiness during Transcendence
[[patches]]
[patches.pattern]
target = "game.lua"
pattern = "G.SHADERS['CRT']:send('glitch_intensity', 0)--0.1*G.SETTINGS.GRAPHICS.crt/100 + (G.screenwipe_amt) + 1)"
position = "at"
payload = '''
	G.SHADERS['CRT']:send('glitch_intensity', transcendence_glitch or 0)
	G.SHADERS['CRT']:send('noise_fac', transcendence_noise or 0)
	G.SHADERS['CRT']:send('bloom_fac', transcendence_bloom or 0)
	G.SHADERS['CRT']:send('crt_intensity', transcendence_crt or 0)
'''
match_indent = true

# Cryptid compatibility
[[patches]]
[patches.pattern]
target = "game.lua"
pattern = "G.SHADERS['CRT']:send('glitch_intensity', glitched_intensity or 0)"
position = "at"
payload = '''
	G.SHADERS['CRT']:send('glitch_intensity', glitched_intensity or 0)
	G.SHADERS['CRT']:send('noise_fac', transcendence_noise or 0)
	G.SHADERS['CRT']:send('bloom_fac', transcendence_bloom or 0)
	G.SHADERS['CRT']:send('crt_intensity', transcendence_crt or 0)
'''
match_indent = true

# Midground sprites
# taken from Cryptid
[[patches]]
[patches.pattern]
target = "card.lua"
pattern = "if self.config.center.soul_pos and (self.config.center.discovered or self.bypass_discovery_center) then"
position = "after"
payload = '''
	if self.config.center.soul_pos and self.config.center.soul_pos.extra and (self.config.center.discovered or self.bypass_discovery_center) then
		local scale_mod = 0.07-- + 0.02*math.cos(1.8*G.TIMERS.REAL) + 0.00*math.cos((G.TIMERS.REAL - math.floor(G.TIMERS.REAL))*math.pi*14)*(1 - (G.TIMERS.REAL - math.floor(G.TIMERS.REAL)))^3
		local rotate_mod = 0--0.05*math.cos(1.219*G.TIMERS.REAL) + 0.00*math.cos((G.TIMERS.REAL)*math.pi*5)*(1 - (G.TIMERS.REAL - math.floor(G.TIMERS.REAL)))^2
		self.children.floating_sprite2:draw_shader('dissolve',0, nil, nil, self.children.center,scale_mod, rotate_mod,nil, 0.1--[[ + 0.03*math.cos(1.8*G.TIMERS.REAL)--]],nil, 0.6)
		self.children.floating_sprite2:draw_shader('dissolve', nil, nil, nil, self.children.center, scale_mod, rotate_mod)
	end
'''
match_indent = true

# Dynatext pulse screen shake 
[[patches]]
[patches.pattern]
target = "engine/text.lua"
pattern = "letter.r = letter.r + (G.SETTINGS.reduced_motion and 0 or 1)*(letter.scale - 1)*(0.02*(-#self.strings[self.focused_string].letters/2 - 0.5 + k))"
position = "after"
payload = '''
if to_number(letter.scale) > 8 and self.config.pulse.shake_screen then
	G.ROOM.jiggle = G.ROOM.jiggle + (to_number(letter.scale)/24)
end
'''
match_indent = true

# Failsafe for rational hyperoperators
# couldnt figure out how to hook it so im doing this garbage instead 
[[patches]]
[patches.pattern]
target = '''=[SMODS Talisman "big-num/omeganum.lua"]'''
pattern = "arrows = Big:create(arrows)"
position = "after"
payload = '''arrows = arrows:ceil()'''
match_indent = true

# Plasma deck stuff
[[patches]]
[patches.pattern]
target = "back.lua"
pattern = '''G.E_MANAGER:add_event(Event({
            func = (function()
                local text = localize('k_balanced')
                play_sound('gong', 0.94, 0.3)
                play_sound('gong', 0.94*1.5, 0.2)
                play_sound('tarot1', 1.5)
                ease_colour(G.C.UI_CHIPS, {0.8, 0.45, 0.85, 1})
                ease_colour(G.C.UI_MULT, {0.8, 0.45, 0.85, 1})
                attention_text({
                    scale = 1.4, text = text, hold = 2, align = 'cm', offset = {x = 0,y = -2.7},major = G.play
                })
                G.E_MANAGER:add_event(Event({
                    trigger = 'after',
                    blockable = false,
                    blocking = false,
                    delay =  4.3,
                    func = (function() 
                            ease_colour(G.C.UI_CHIPS, G.C.BLUE, 2)
                            ease_colour(G.C.UI_MULT, G.C.RED, 2)
                        return true
                    end)
                }))
                G.E_MANAGER:add_event(Event({
                    trigger = 'after',
                    blockable = false,
                    blocking = false,
                    no_delete = true,
                    delay =  6.3,
                    func = (function() 
                        G.C.UI_CHIPS[1], G.C.UI_CHIPS[2], G.C.UI_CHIPS[3], G.C.UI_CHIPS[4] = G.C.BLUE[1], G.C.BLUE[2], G.C.BLUE[3], G.C.BLUE[4]
                        G.C.UI_MULT[1], G.C.UI_MULT[2], G.C.UI_MULT[3], G.C.UI_MULT[4] = G.C.RED[1], G.C.RED[2], G.C.RED[3], G.C.RED[4]
                        return true
                    end)
                }))
                return true
            end)
        }))'''
position = "after"
payload = '''G.E_MANAGER:add_event(Event({
            func = (function()
                local text = localize('k_balanced')
                play_sound('gong', 0.94, 0.3)
                play_sound('gong', 0.94*1.5, 0.2)
                play_sound('tarot1', 1.5)
				G.GAME.may_override_monitor_colors = true
                ease_colour(G.C.UI_CHIPS, {0.8, 0.45, 0.85, 1})
                ease_colour(G.C.UI_MULT, {0.8, 0.45, 0.85, 1})
                attention_text({
                    scale = 1.4, text = text, hold = 2, align = 'cm', offset = {x = 0,y = -2.7},major = G.play
                })
                G.E_MANAGER:add_event(Event({
                    trigger = 'after',
                    blockable = false,
                    blocking = false,
                    delay =  4.3,
                    func = (function() 
                            ease_colour(G.C.UI_CHIPS, G.C.BLUE, 2)
                            ease_colour(G.C.UI_MULT, G.C.RED, 2)
                        return true
                    end)
                }))
                G.E_MANAGER:add_event(Event({
                    trigger = 'after',
                    blockable = false,
                    blocking = false,
                    no_delete = true,
                    delay =  6.3,
                    func = (function() 
                        G.C.UI_CHIPS[1], G.C.UI_CHIPS[2], G.C.UI_CHIPS[3], G.C.UI_CHIPS[4] = G.C.BLUE[1], G.C.BLUE[2], G.C.BLUE[3], G.C.BLUE[4]
                        G.C.UI_MULT[1], G.C.UI_MULT[2], G.C.UI_MULT[3], G.C.UI_MULT[4] = G.C.RED[1], G.C.RED[2], G.C.RED[3], G.C.RED[4]
                        return true
                    end)
                }))
				G.GAME.may_override_monitor_colors = false
                return true
            end)
        }))'''
match_indent = true

# Rational Poker Hand level fixes
[[patches]]
[patches.pattern]
target = "functions/common_events.lua"
pattern = "colours = {(to_big(G.GAME.hands[cfg.hand_type].level)==to_big(1) and G.C.UI.TEXT_DARK or G.C.HAND_LEVELS[to_number(to_big(math.min(7, G.GAME.hands[cfg.hand_type].level)))])}"
position = "at"
payload = '''colours = {(to_big(G.GAME.hands[cfg.hand_type].level):floor() == to_big(1) and G.C.UI.TEXT_DARK or to_big(G.GAME.hands[cfg.hand_type].level):floor() == to_big(0) and G.C.RED or G.C.HAND_LEVELS[to_number(to_big(math.min(7, G.GAME.hands[cfg.hand_type].level)):floor())])}'''
match_indent = true
overwrite = true

[[patches]]
[patches.pattern]
target = "functions/common_events.lua"
pattern = '''G.hand_text_area.hand_level.config.colour = G.C.HAND_LEVELS[math.min(vals.level, 7)]'''
position = "at"
payload = '''G.hand_text_area.hand_level.config.colour = G.C.HAND_LEVELS[math.min(math.floor(vals.level), 7)]'''
match_indent = true
overwrite = true

[[patches]]
[patches.pattern]
target = "functions/common_events.lua"
pattern = '''G.hand_text_area.hand_level.config.colour = G.C.HAND_LEVELS[to_number(to_big(math.min(vals.level, 7)))]'''
position = "at"
payload = '''G.hand_text_area.hand_level.config.colour = G.C.HAND_LEVELS[to_number(to_big(math.min(math.floor(vals.level), 7)))]'''
match_indent = true
overwrite = true

# Make Cryptid Redeemed Deck not give Transcend Vouchers
[[patches]]
[patches.pattern]
target = '''=[SMODS Cryptid "items/deck.lua"]'''
pattern = "if vv == self.config.center.key then"
position = "at"
payload = '''if vv == self.config.center.key and v.key ~= 'v_may_transcend_1' and v.key ~= 'v_may_transcend_2' then'''
match_indent = true

# make table of secret hands
[[patches]]
[patches.pattern]
target = '''game.lua'''
pattern = "G.FUNCS.blind_chip_UI_scale(G.hand_text_area.blind_chips)"
position = "after"
payload = '''
	may.secret_hands = {}
	for k, v in pairs(G.GAME.hands) do
		if not v.visible then
			table.insert(may.secret_hands, k)
		end
	end
'''
match_indent = true

# Intrusive Entropy fixes again
[[patches]]
[patches.pattern]
target = '''functions/misc_functions.lua'''
pattern = "G.ARGS.score_intensity.earned_score = to_number(math.min(Entropy.get_chipmult_score(G.GAME.current_round.current_hand.chips, G.GAME.current_round.current_hand.mult), 1e300))"
position = "at"
payload = '''G.ARGS.score_intensity.earned_score = get_final_score(to_big(G.GAME.current_round.current_hand.chips), to_big(G.GAME.current_round.current_hand.mult))'''
match_indent = true
overwrite = true

[[patches]]
[patches.pattern]
target = '''=[SMODS Entropy "lib/utils.lua"]'''
pattern = '''
	function Entropy.get_chipmult_score(hand_chips, mult)
		local operator = math.max(get_final_operator(), -1)
		if Entropy.score_cache[number_format(hand_chips).."x"..number_format(mult)] then return Entropy.score_cache[number_format(hand_chips).."x"..number_format(mult)] end
		if operator <= -2 then return to_big(1) / to_big(0)
		elseif operator == -2 then hand_chips=to_big(mult); return hand_chips
		elseif operator == -1 then return to_big(hand_chips)+to_big(mult) 
		elseif operator == 0 then return to_big(hand_chips)*to_big(mult) end
		local ret = (operator and to_big(hand_chips):arrow(operator, to_big(mult)) or to_big(hand_chips)*to_big(mult))
		Entropy.score_cache[number_format(hand_chips).."x"..number_format(mult)] = ret
		return ret
	end
'''
position = "at"
payload = '''
	function Entropy.get_chipmult_score(hand_chips, mult)
		return get_final_score(hand_chips, mult)
	end

'''
match_indent = true
overwrite = true